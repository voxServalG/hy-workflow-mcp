schema: hy-workflow.protocol.v1
obligations:
  - id: INV-EXAMPLE-001
    kind: invariant
    status: active
    statement: Public configuration files remain backward compatible with the documented schema.
    sources:
      - docs/invariants/INV-EXAMPLE-001.md
      - docs/incidents/INC-EXAMPLE-001.md
    applies_to:
      paths:
        - src/config/**
        - schemas/**
    verification:
      scale: medium
      commands:
        - argv: ["npm", "run", "test:config-contract"]
          expected_exit_code: 0
